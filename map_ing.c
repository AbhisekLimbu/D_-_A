#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <assert.h>
typedef struct{
	char* char_t;
	char* value;
	size_t size;
}map_t;

void init_map(map_t* x){
	x -> size = 0;
	

}
void insert_v(map_t* x, char** char_t, char** value){
	assert(*char_t != "" && *value != "");
	*char_t = *v;
}





int main(){
	map_t map;
	map_t* map = &map;

	return 0;
}
